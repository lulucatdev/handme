# Handme — 产品理念设计文档

## 一句话

把文件递给自己。

---

## 问题

在 macOS 上工作时，文件散落在终端、Finder、各种应用之间。你在终端里找到了一个文件，想一会儿再处理；你编译出了一个产物，想发给别人；你下载了几张图，想稍后整理。这些文件没有一个"暂存"的地方——它们要么淹没在桌面上，要么被遗忘在某个深层目录里。

核心矛盾是：**你知道这个文件在哪，但你没有一个地方"放"它。**

---

## 理念

Handme 是一个**文件收件箱**。它不管理文件，不移动文件，不复制文件。它只是让你把文件"标记"一下——"这个，待会儿要用"——然后在你需要的时候，快速取用。

它像是你桌子角上的一个托盘。你把东西往里一扔，回头再拿。

它填补的是 **Finder（持久存储）** 和 **剪贴板（瞬时存储）** 之间的空白——一个轻量的、有一定持续时间的文件暂存区。

---

## 使用场景

**从命令行到图形界面。** 在终端里输入 `handme report.pdf`，这份文件就出现在菜单栏的托盘里。你可以从那里拖到邮件、拖到聊天窗口、拖到任何地方。

**临时收集。** 你正在不同文件夹里挑选几个文件，每找到一个就 `handme` 一下。最后它们都在一个列表里，可以一起操作。

**构建产物的中转。** 编译完成后，脚本自动把产物 `handme` 进来。你不用记路径，不用翻 Finder，它就在托盘里等你。

**跨应用传递。** 在一个应用里看到一个文件，想在另一个应用里打开。把它 handme 进来，然后从托盘拖出去。

---

## 产品的层次

Handme 由三层组成，按重要性排列：

1. **CLI 命令行工具** — 这是文件进入收件箱的主要入口。没有它，收件箱是空的，应用没有意义。
2. **主窗口应用** — 这是用户查看、操作收件箱的主要界面。它是一个正常的 macOS 应用，有 Dock 图标，有窗口，有菜单栏。
3. **菜单栏托盘面板** — 这是一个快捷入口，让用户不用打开主窗口就能快速查看和操作文件。它是锦上添花，不是核心。

**首先是 CLI + 主应用，其次才是托盘。** 如果只有托盘没有主窗口，它不像一个完整的应用；如果只有主窗口没有 CLI，文件进不来。托盘面板是在前两者都稳定之后的便捷补充。

---

## 它应该是什么样的

### 主窗口

主窗口是 Handme 的核心界面。它是你管理收件箱的地方——可以看到所有文件、进行批量操作、调整设置。

应用启动时，主窗口应该打开。它应该是一个正常的 macOS 窗口，有标题栏，在 Dock 里有图标。关闭主窗口后，应用退到菜单栏继续运行。这和 macOS 用户的预期一致——关窗口不是退出应用。

### 菜单栏托盘

菜单栏里有一个安静的图标。当有文件进来时，它应该有一点点视觉变化——让你知道"有东西等着你"，但不至于分散注意力。

点击图标弹出一个小面板，列出最近放进来的文件。像是 macOS 原生的菜单栏面板一样自然。它是主窗口的一个轻量替代，用于快速查看和操作，不用特意打开主窗口。

应用启动时，主窗口应该打开。关闭主窗口后，应用退到菜单栏继续运行。这和 macOS 用户的预期一致——关窗口不是退出应用。

### 文件列表

每个文件显示：
- 它的图标（系统图标，不是自定义的）
- 文件名
- 来源路径（缩短显示）
- 什么时候加进来的

如果文件已经不存在了（被移动或删除），应该明确标记出来，而不是假装它还在。

### 预览

选中文件后按空格键，应该能调起系统的 QuickLook 预览。这是 macOS 用户处理文件时最本能的操作，Handme 不应该打破这个习惯。

### 操作

对文件可以做的事情很少，而且每一个都应该很快：

- **打开**——用系统默认应用打开
- **在 Finder 中显示**——跳转到文件所在目录
- **拖出去**——从列表直接拖到任何接受文件的地方
- **复制**——把文件本身放到剪贴板
- **复制路径**——把文件路径放到剪贴板
- **移除**——从收件箱中去掉（不删除原文件）
- **清空**——清空整个收件箱

没有重命名、没有移动、没有标签、没有分类。收件箱就是一个线性列表，按时间排序。

当文件超过十几个时，应该可以通过键入几个字母快速过滤定位。

### 多选

可以选中多个文件，然后一起拖出去、一起复制、一起移除。选中方式应该和 Finder 一致——Cmd+点击切换选中、Shift+点击范围选择、Cmd+A 全选。

### 添加文件

添加文件的方式应该尽可能简单：

- 命令行：`handme <文件>`，支持多个文件、通配符
- 拖拽：把文件拖到菜单栏图标上（如果技术上可行）
- 快捷键：全局快捷键打开面板

不需要在应用内"添加"文件。Handme 的入口在外面——在终端里、在 Finder 里、在任何你正在工作的地方。

**重复添加：** 如果同一个文件被 handme 了两次，不应出现两个条目。应该把已有的条目更新时间并置顶。

**命令行反馈：** 在终端输入 `handme file.zip` 后，应该立即给出简洁的确认，比如 `✓ file.zip`。不需要多余的话。

### CLI 安装

命令行工具是 Handme 最重要的入口。用户第一次打开应用时，应该能一键安装 `handme` 命令。安装过程不需要 sudo，不需要改系统目录。

### 快捷键

菜单栏面板应该可以通过全局快捷键呼出/隐藏。在面板和主窗口内，所有操作都应该可以通过键盘完成。快捷键应该可以自定义。

### 持久化

收件箱的内容在应用重启、系统重启后应该保留，直到用户主动移除或清空。这些文件是用户有意放进来的，不应该悄悄消失。

### 容量

收件箱应该有一个上限。当文件数量达到上限时，最早加入的文件自动移出。这个上限应该可以设置，但默认值应该足够大，让用户在正常使用中感觉不到它的存在。

---

## 它不应该是什么

- **不是文件管理器。** 不浏览目录，不创建文件夹，不移动文件。
- **不是收藏夹。** 文件放进来是为了很快用掉，不是永久保存。
- **不是同步工具。** 不跨设备，不上传，不备份。
- **不是剪贴板管理器。** 不记录剪贴板历史，不处理文本片段。
- **不是一个后台守护进程。** 它是一个正常的 macOS 应用，有窗口、有 Dock 图标（主窗口打开时）、有菜单栏图标。

---

## 感受

用 Handme 应该像是**递东西给自己**。你在这边找到了一个文件，"递"一下，然后在那边接住。整个过程不需要思考路径、不需要打开 Finder、不需要切换窗口。

它应该快。添加一个文件不应超过一秒。打开面板不应有任何延迟。拖拽应该即时响应。

它应该小。不占用太多屏幕空间，不消耗太多系统资源，不需要太多配置。

它应该可靠。文件指向应该始终准确。如果文件被移动了，告诉用户而不是静默失败。
